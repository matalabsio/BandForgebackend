[pytest]
# Run from backend/ with the project venv (Python 3.10+). See README Testing.
pythonpath = .
minversion = 7.0
markers =
    integration: live external API tests (set RAZORPAY_LIVE_TEST=1 to run)
