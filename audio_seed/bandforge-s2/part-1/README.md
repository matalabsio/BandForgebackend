# BandForge Listening Section 2 audio

Place the founder file here before upload:

```text
full.mp3   ← copy from test/listening/audio/Listening_S2_Audio.mp3
```

Upload:

```bash
cd backend && source .venv/bin/activate
python -m scripts.upload_listening_audio --preset bandforge-s2
```

R2 key: `listening/bandforge-s2/part-1/full.mp3`
